defmodule ConfigTest do
  use ExUnit.Case

  test "lookup Ben Carter" do
   #IO.inspect Facebex.API.User.search_by_name("Ben Carter")
  end

  test "lookup with full details" do
  #	IO.inspect Facebex.API.User.search_by_name("Ben Carter", :full_user)
  end

end